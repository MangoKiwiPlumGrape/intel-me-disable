# intel-me-disable
Soft-disables the Intel Management Engine (ME/CSME/GSC) at the OS level on Debian/Ubuntu — blocks all ME kernel modules and removes the device from lspci without touching firmware.
